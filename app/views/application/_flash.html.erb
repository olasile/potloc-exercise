<% flash.each do |type, msg| %>
  <div class="alert alert-<%= flash_alert_class(type) %> alert-dismissible" role="alert">
    <%= msg %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>